package de.lutana.geodataextractor.entity;

/**
 * An object that has location information (get location only).
 * 
 * @author Matthias Mohr
 */
public interface Located {
	
	public Location getLocation();
	
}
